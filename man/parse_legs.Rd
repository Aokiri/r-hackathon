% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_legs.R
\name{parse_legs}
\alias{parse_legs}
\title{Parse route legs from a single API response into a tidy data frame}
\usage{
parse_legs(response, from_station_id, to_station_id)
}
\arguments{
\item{response}{List returned by \code{get_route}.}

\item{from_station_id}{Origin station ID, added as a column for identification.}

\item{to_station_id}{Destination station ID, added as a column for identification.}
}
\value{
A data frame with one row per leg: from_station_id, to_station_id,
  connection_id, leg_id, from_stop, from_stop_id, to_stop, to_stop_id,
  mode, line.
}
\description{
Extracts one row per leg (vehicle segment or walk) for every connection in
the response. Use \code{dplyr::bind_rows(lapply(..., parse_legs, ...))} to
combine results across multiple queries.
}
\examples{
legs_df <- data.frame(name = "Lausanne", stopid = "8501120",
                      type = "express_train", line = "RE33",
                      stringsAsFactors = FALSE)
legs_df$exit <- data.frame(name = "Geneva", stopid = "8501008",
                           stringsAsFactors = FALSE)
resp <- list(connections = data.frame(departure = "2026-06-19 08:00:00",
                                      stringsAsFactors = FALSE))
resp$connections$legs <- list(legs_df)
parse_legs(resp, "8501120", "8501008")
}
